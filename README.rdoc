== README

* git clone git@github.com:amberwebb/rspec_example.git
* cd rspec_example
* bundle install
* rake db:migrate
* rake db:test:prepare

To run tests

* rspec

<p>All tests are located in the spec folder</p>
